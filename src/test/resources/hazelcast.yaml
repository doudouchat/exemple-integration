hazelcast:
   config:
      network:
         port: ${hazelcast.port}
         port-auto-increment: false
         join:
            tcp-ip:
               enabled: true
            multicast:
               enabled: false
hazelcast-client:
   network:
      cluster-members:
      - 127.0.0.1:${hazelcast.port}